import Image from 'next/image';

export default function Footer({ copyrightText }) {
  return (
    <footer className="flex flex-col items-center py-16">
      {/* <Image
        src="/bottomcloud.png" // Path relative to the 'public' folder
        alt="Example Image"
        width={500}   // Define width
        height={300}  // Define height
        className=""
      /> */}
    </footer>
  );
}
